# Commodity ETFs — render under a _Commodities_ sub-header in the
# "Index, Sector & Macro Returns" Slack section (below _Tech Themes_).
# Regular total-return ETFs: go through the normal _format_etf_line()
# path with prior-year/YTD returns, like the sector/tech groups.
# Added 2026-06-03 (GLD, per the "gold index price return" request).
GLD
# BTC-USD = bitcoin spot price (yfinance 24/7 pair, like CL=F/^TNX have special
# chars). Added 2026-06-06 per the "add bitcoin price, next to gold" request.
# Trades 7 days/wk, so its trailing-252 daily-return z-score spans ~252 calendar
# days vs ~365 for the weekday ETFs — its own distribution, which is correct for
# BTC. Smoke-tested: downloads clean, day%/YTD/52w-range all compute.
BTC-USD
