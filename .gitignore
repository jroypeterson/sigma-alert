.claude/
__pycache__/
*.pyc
.pytest_cache/
*.tmp.*
.env
# Derived/runtime artifact — the interactive return-map HTML is regenerated on
# every screener run (and on demand via scripts/return_map.py) from the
# committed cache/returns_snapshot.json, so it isn't tracked.
readable/

# Codex review output — kept on disk as proof-of-run, but untracked.
# If tracked/untracked, each review appears in the next review's working-tree
# diff and pollutes it with hundreds of KB of prior review text (2026-07-20).
codex_feedback/
